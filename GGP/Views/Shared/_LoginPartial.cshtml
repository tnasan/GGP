<ul class="nav navbar-nav navbar-right">
    @if (AccountHelper.IsAuthenticated)
    {
        <li class="dropdown">
            <a href="javascript: void();" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                ยินดีต้อนรับ @AccountHelper.CurrentUsername <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
                <li>@Html.ActionLink("เปลี่ยนรหัส", "ChangePassword", "Account", null, null)</li>
            </ul>
        </li>
        <li>
            @Html.ActionLink("ออกจากระบบ", "Logout", "Authentication")
        </li>
    }
    else
    {
        <li>
            @Html.ActionLink("เข้าสู่ระบบ", "Login", "Authentication")
        </li>
    }
</ul>